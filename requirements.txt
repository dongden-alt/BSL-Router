# Dependencies for BSL Router
# Pinned to the validated production stack (2026-09-12): the suite (2729 tests)
# and the live :6969 router run on these exact versions. Unbounded floors let CI
# resolve bleeding-edge FastAPI/Starlette and broke route iteration (_IncludedRouter).
fastapi==0.111.0
uvicorn[standard]==0.30.1
httpx[http2]==0.27.0
pydantic==2.7.4
PyYAML==6.0.1
cryptography==44.0.3
python-multipart==0.0.31
mitmproxy==11.0.2
dnspython==2.8.0
openpyxl==3.1.5
